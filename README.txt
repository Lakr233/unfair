# UNFAIR

Licensed under MIT, requires macOS <= 11.2.3 on Apple Silicon.

./unfair --help for more details.

Build:
  make

Install:
  make install

The default prefix is /usr/local. Use PREFIX=/path to install elsewhere.
If /usr/local is not writable, run sudo make install.
